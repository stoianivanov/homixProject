import React, { Component } from "react";
import ReactDOM from "react-dom";

const ProjectRole = () => (
  <div>
    <span> SomeProjectName</span>
    <span> Position in the Team</span>
  </div>
);

export default ProjectRole;
